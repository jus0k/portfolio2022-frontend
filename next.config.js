/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  images: {
    loader: "default",
    domains: ["localhost"],
  },
}

module.exports = nextConfig

module.exports = {
  images: {
    domains: ["secret-thicket-10512.herokuapp.com"],
  },
}
